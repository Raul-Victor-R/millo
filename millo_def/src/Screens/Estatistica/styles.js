import { StyleSheet } from "react-native/types";

export default StyleSheet.create({
   container: {
      flex: 1,
      alignItems: 'center',
      backgroundColor: '#000',
   },
   text: {
      color: '#ffff'
   }
})